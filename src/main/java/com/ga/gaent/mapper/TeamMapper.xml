<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ga.gaent.mapper.TeamMapper">
    
    <!-- 조직도 정보 조회 -->
    <select id="selectTreeInfo" resultType="Map">
        SELECT
            team_code  id,
            parent_team_code parent,
            team_name text
        FROM
            team
        
        UNION ALL

        SELECT
            emp_code id,
            team_code parent,
            emp_kor_name text
        FROM
            emp
        ORDER BY
            parent, id, text
    </select>
    
    <!--  팀 등록 -->
    <insert id="insertTeam" parameterType="com.ga.gaent.vo.TeamVO">
        INSERT INTO team(
            team_code,
            team_name,
            team_des,
            parent_team_code,
            team_create_date,
            team_update_date
        )VALUES(
            #{teamCode},
            #{teamName},
            #{teamDes},
            #{parentTeamCode},
            NOW(),
            NOW()
        )
    </insert>
</mapper>